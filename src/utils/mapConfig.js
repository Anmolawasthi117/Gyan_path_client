// These values should match your actual image and grid
export const MAP_WIDTH = 1000;  // pixels (match your PNG size)
export const MAP_HEIGHT = 1000;

export const GRID_WIDTH = 30;   // max X in your node data
export const GRID_HEIGHT = 22;  // max Y in your node data

export const SCALE_X = MAP_WIDTH / GRID_WIDTH;
export const SCALE_Y = MAP_HEIGHT / GRID_HEIGHT;

